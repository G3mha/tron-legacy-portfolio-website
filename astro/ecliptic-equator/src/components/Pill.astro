<div class="pill"><slot /></div>

<style>
	.pill {
		display: flex;
		padding: 0.5rem 1rem;
		gap: 0.5rem;
		color: #000000;
		border: 1px solid var(--accent-regular);
		background-color: var(--accent-regular);
		border-radius: 999rem;
		font-size: var(--text-md);
		font-weight: 700;  /* Added this line - makes text bold */
		line-height: 1.35;
		white-space: nowrap;
	}
</style>
